apply plugin : 'java'
apply plugin: 'eclipse'

repositories {
  mavenCentral()
}

dependencies {
  testCompile 'org.mockito:mockito-all:1.9.0', 'junit:junit:4.5'
}

sourceSets {
  main {
    java {
      srcDir 'src'
      }
    }
  test {
    java {
      srcDir 'test'
    }
  }
}
